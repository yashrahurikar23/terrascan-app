# Try to install GDAL - if system libraries are available, this might work
# If it fails, the app will handle it gracefully
gdal>=3.8.0
streamlit>=1.28.0
numpy>=1.24.0
pillow>=10.0.0
matplotlib>=3.7.0
plotly>=5.14.0
pandas>=2.0.0
scipy>=1.10.0
