# GDAL - try to install, but make it optional for cloud platforms
# If system python3-gdal is installed, we can use that instead
streamlit>=1.28.0
numpy>=1.24.0
pillow>=10.0.0
matplotlib>=3.7.0
plotly>=5.14.0
pandas>=2.0.0
scipy>=1.10.0
