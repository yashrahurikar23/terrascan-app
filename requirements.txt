# GDAL removed - not installable on Streamlit Cloud due to system dependency conflicts
# For full GDAL functionality, use Docker deployment (see Dockerfile)
# The app handles missing GDAL gracefully
streamlit>=1.28.0
numpy>=1.24.0
pillow>=10.0.0
matplotlib>=3.7.0
plotly>=5.14.0
pandas>=2.0.0
scipy>=1.10.0
